[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."cleanup-student-data"]
  schedule = "@daily"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
